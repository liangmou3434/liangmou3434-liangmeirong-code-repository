#define _CRT_SECURE_NO_WARNINGS 1
#include<stdio.h>
#include<stdlib.h>
//int main()
//{
//	char str[20] = { 0 };//对一个字符串进行初始化
//	scanf("%s", str);//遇到空格会停止读取 如hello world
//	printf("%s\n", str);//打印字符串用%s  只读取空格前的部分最后打印 hello
//	return 0;
//}


//用gets();函数来解决这个问题

//int main()
//{
//	char str[20] = { 0 };//对一个字符串进行初始化
//	gets(str);//读取完整的字符串
//	//scanf("%s", str);//遇到空格会停止读取 如hello world
//	printf("%s\n", str);//打印字符串用%s  只读取空格前的部分最后打印 hello
//	return 0;
//}

